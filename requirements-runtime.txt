# P1-R Runtime Validation Requirements
# 用于在目标环境中验证 P1-R 实现

# Core dependencies - FIXED VERSIONS
torch==2.1.0
vllm==0.6.3.post1

# Config and serialization
pyyaml==6.0.1

# Network
requests==2.31.0

# Metrics (optional but recommended)
prometheus-client==0.19.0

# Testing
pytest==7.4.3
pytest-asyncio==0.21.1
